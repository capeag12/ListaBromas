import { BanBadLanguajePipe } from './ban-bad-languaje.pipe';

describe('BanBadLanguajePipe', () => {
  it('create an instance', () => {
    const pipe = new BanBadLanguajePipe();
    expect(pipe).toBeTruthy();
  });
});
